package com.anshumandhal.fakestore.data.local

import androidx.room.Dao

@Dao
interface ProductDao {

/*    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insertProductId(
        productId: Int
    )

    @Delete
    suspend fun deleteProductId(
        productId: Int
    )*/

}